<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sem6finalproject">

    <application
        android:allowBackup="true"
        android:icon="@drawable/cvclogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/cvclogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".donationform"></activity>
        <activity android:name=".volunteerDashboard" />
        <activity android:name=".SigninActivity3" />
        <activity android:name=".SigninActivity2" /> <!-- <activity android:name=".signupactivity" /> -->
        <activity android:name=".SigninActivity1" />
        <activity
            android:name=".LoginConformationActivity"
            android:noHistory="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- <activity android:name=".LoginActivity" /> -->
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".MainActivity"
            android:noHistory="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>